/* z/op/drop.c
**
** This file is in the public domain.
*/
    /* c3__drop: move a result down on the cap.
    */
#     define _zn_bip_drop(field) *_zn_anvil(z, ray_bip, drop, field)

      case c3__drop: {
        u3_fox gus = _zn_bip_drop(d.gus);

        _zn_complete(z, _zn_bip_drop(f.c.ray_lid), gus);
        break;
      }
